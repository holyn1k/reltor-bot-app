name: Android Build

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7

      # Создаем файл настроек, который принудительно укажет версию плагина Android
      - name: Create Settings & Init
        run: |
          echo "pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
              resolutionStrategy {
                  eachPlugin {
                      if (requested.id.id == 'com.android.application') {
                          useModule('com.android.tools.build:gradle:8.2.2')
                      }
                  }
              }
          }" > settings.gradle
          
          echo "allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }" > init.gradle

      - name: Build with Gradle
        run: |
          cd app
          # Копируем созданные файлы в папку приложения и собираем
          cp ../settings.gradle .
          gradle assembleDebug --init-script ../init.gradle --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: "**/build/outputs/apk/debug/*.apk"
